# RelayRoom.Web

React + Vite + TypeScript UI. Package manager is **pnpm** (Corepack).

```bash
corepack pnpm install
corepack pnpm dev
```

The Vite proxy forwards `/api` and `/hubs` to `http://localhost:5090`.
